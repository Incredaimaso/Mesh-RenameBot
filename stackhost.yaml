user:
  name: cevilion
  github: incredaimaso

language: "python"

commands:
  build:
    - "pip3 install -r requirements.txt"
  package:
    - "nano inxi"
    - "python3"
    - "FFmpeg"
  start:
    - "python3 -m MeshRenameBot"
  env:
    - "ENVIRONMENT=production"